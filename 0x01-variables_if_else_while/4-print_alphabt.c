#inlcude <stdio.h>
/**
 * main - Entry point
 *
 * return: Always 0 (Success/corrrect)
 */
int main(void)
{
  char alphabet;
  for (alphabet = 'a'; alphabet <= 'z'; alphabet++)
    {
      if (aplhabet == 'q')
	continue;
      else if (alphabet == 'e')
	continue;
      putchar(alphabet);
    }
  putchar('\n');
  return (0);
}
